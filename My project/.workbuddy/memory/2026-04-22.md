# 2026-04-22 工作日志

## MiniGameManager 重构完成
- **删除** `MiniGameManager.cs`（含 `MiniGameType` 枚举），将小游戏管理职责合并到 `UIControler`
- **UIControler.cs** 新增 `StartMiniGame(int gameType, int bridgeIndex)` 和 `CloseMiniGame()` 两个静态方法
- **CatUbteract.cs** 删除 `isMiniGameRunning`、`SaveReturnPosition()`、`OnMiniGameClosed()`、Update 守卫，三处小游戏触发简化为 `UIControler.StartMiniGame()`
- **QuizManager.cs / PuzzleMG.cs / Event.cs** 删除 `MiniGameManager.Instance.HideMiniGame()` 调用，只保留 `UIControler.AwardPendingFish()`
- **ButtonEvent.cs / SceneJump.cs** 删除 `MiniGameManager` 分支逻辑，只保留场景切换
- 全局搜索确认：`Assets/Scripts` 下 `MiniGameManager` 和 `MiniGameType` 引用数为 0
